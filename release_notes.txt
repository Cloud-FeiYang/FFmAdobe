## FFmAdobe 26w21c (Dev Branch)

### 修复
- **修复硬件编码器（如 NVENC）验证失败的 Bug**：由于 NVENC 等硬件加速编码器对输入视频有最低分辨率限制（例如 144x144），此前验证阶段写死的 64x64 黑屏测试源会导致其被显卡驱动拒绝并误报为“不支持”。现在，验证阶段将严格使用你当前导出的真实分辨率（如 1920x1080）来生成测试黑屏，完美兼容各种硬件编码器。